class Attendance {
  final String studentId;
  final DateTime date;
  final bool isPresent;

  Attendance({
    required this.studentId,
    required this.date,
    required this.isPresent,
  });
}
